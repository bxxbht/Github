

这里有几点：
1.共享内存；
2.循环数组；
3.函数入时，将函数名追加到数组末；
  函数出时，先判断最后数组末是否同名入函数，若是，清除最后数组内容，否则，继续追加到数组末；

问题：
单线程时，就是一个典型的堆栈使用；
多线程时，由于A线程函数funA()入时，之前可能有B线程的函数funB()入，这时A线程的函数funA()出时就有可能造成函数积压；
	对于积压的处理，循环数组指针会继续从0处理。

往往定位问题时，都只关心最后的几个数据即可。
