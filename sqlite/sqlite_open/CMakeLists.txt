Project(SQLITE)

SET(SRC_LIST sqlite.cpp)

MESSAGE(STATUS "Thist is SOURCE dir" ${SQLITE_SOURCE_DIR})
MESSAGE(STATUS "Thist is BINARY dir" ${SQLITE_BINARY_DIR})

LINK_DIRECTORIES(/usr/lib)

ADD_EXECUTABLE(sqlite ${SRC_LIST})

TARGET_LINK_LIBRARIES(sqlite sqlite3)
